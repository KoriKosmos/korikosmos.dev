services:
  web:
    build: .
    ports:
      - "8484:4321"
      - "8081:8081"
    environment:
      - LASTFM_USER=${LASTFM_USER}
      - LASTFM_API_KEY=${LASTFM_API_KEY}
    volumes:
      - scores_data:/app/data
    restart: unless-stopped

volumes:
  scores_data:
