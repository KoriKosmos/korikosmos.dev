---
const { project } = Astro.props;
---
<a href={`/portfolio/${project.slug}/`} class="block bg-base-200/60 rounded-lg p-4 hover:bg-base-200 transition">
  <h2 class="text-xl font-semibold mb-2">{project.data.title}</h2>
  <p class="text-base-content/60">{project.data.summary}</p>
</a>
